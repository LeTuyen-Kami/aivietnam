---
phase: 01-stream-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/stream/server.ts
  - src/lib/stream/user.ts
  - src/app/(frontend)/api/stream/token/route.ts
  - tests/int/stream-user-id.int.spec.ts
autonomous: true
requirements:
  - STRM-01
  - STRM-02
  - STRM-03
user_setup: []
must_haves:
  truths:
    - POST /api/stream/token returns 401 when there is no Payload session.
    - Authenticated POST returns JSON with keys `token` and `expiresAt` (ISO string).
    - Stream user id equals `String(user.id)` for the authenticated user (STRM-03).
  artifacts:
    - src/lib/stream/server.ts exists and exports getStreamServerClient (or equivalent).
    - src/app/(frontend)/api/stream/token/route.ts exports POST.
    - tests/int/stream-user-id.int.spec.ts exists and passes.
  key_links:
    - Token route uses payload.auth with request headers; user id for Stream comes only from authenticated user.
---

<objective>
Implement lazy Stream `StreamClient` singleton (STRM-01), authenticated `POST /api/stream/token` that upserts the Stream user and returns a short-lived JWT with `expiresAt` (STRM-02), and a documented pure mapping `String(user.id)` (STRM-03).

Purpose: Delivers roadmap success criteria for Phase 1.

Output: Server modules under `src/lib/stream/`, token route, unit test for mapping.
</objective>

<execution_context>
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/workflows/execute-plan.md
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-stream-foundation/01-CONTEXT.md
@.planning/phases/01-stream-foundation/01-RESEARCH.md
@.planning/phases/01-stream-foundation/01-PATTERNS.md
@src/app/(frontend)/api/site-comments/route.ts
@src/collections/Users/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stream server singleton</name>
  <files>src/lib/stream/server.ts</files>
  <read_first>src/payload.config.ts, .planning/phases/01-stream-foundation/01-CONTEXT.md, package.json</read_first>
  <action>Create `src/lib/stream/server.ts` with `import 'server-only'` at top. Read `process.env.STREAM_API_KEY` and `process.env.STREAM_API_SECRET`; if either missing at runtime, throw a clear Error (message must include `STREAM_API`). Export `getStreamServerClient(): StreamClient` that lazily constructs `new StreamClient(apiKey, apiSecret)` from `@stream-io/node-sdk`. Do not export secret values. Match D-11.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/stream/server.ts` contains `import 'server-only'`
    - `src/lib/stream/server.ts` contains `StreamClient`
    - `src/lib/stream/server.ts` contains `STREAM_API_KEY`
  </acceptance_criteria>
  <done>Singleton compiles; no client bundle import path exists</done>
</task>

<task type="auto">
  <name>Task 2: Mapping helper STRM-03</name>
  <files>src/lib/stream/user.ts</files>
  <read_first>src/payload-types.ts, .planning/phases/01-stream-foundation/01-CONTEXT.md</read_first>
  <action>Create `src/lib/stream/user.ts` exporting `streamUserIdFromPayloadUser(user: User): string` returning `String(user.id)`. Add JSDoc: "STRM-03: Payload user.id is the canonical Stream user id." If you add `streamDisplayName(user)` returning `user.name ?? user.email ?? user.id` for upsert, keep it colocated.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/stream/user.ts` contains `streamUserIdFromPayloadUser`
    - `src/lib/stream/user.ts` contains `String(user.id)`
  </acceptance_criteria>
  <done>Pure helpers ready for route and tests</done>
</task>

<task type="auto">
  <name>Task 3: POST /api/stream/token route</name>
  <files>src/app/(frontend)/api/stream/token/route.ts</files>
  <read_first>src/app/(frontend)/api/site-comments/route.ts, src/lib/stream/server.ts, src/lib/stream/user.ts, .planning/phases/01-stream-foundation/01-CONTEXT.md</read_first>
  <action>Create Route Handler at exact path `src/app/(frontend)/api/stream/token/route.ts`. Implement `export async function POST(req: NextRequest)` only. Flow: (1) `const payload = await getPayload({ config })` with `import config from '@payload-config'`; (2) `const headersList = await headers()` from `next/headers`; (3) `const { user } = await payload.auth({ headers: headersList })`; (4) if `!user` return `NextResponse.json({ error: 'Unauthorized' }, { status: 401 })` — body must contain the word `Unauthorized` or use `{ error: string }` shape consistent with other APIs; (5) resolve `User` document if needed (if `user` is not full User, `findByID` users collection with `overrideAccess: false` and pass `user` from auth per project security rules — if `auth()` already returns sufficient id/email/name for site member, use `getSiteMemberUser` from `@/access/siteMemberUser` when applicable); (6) `const client = getStreamServerClient()`; (7) `const streamUserId = streamUserIdFromPayloadUser(memberOrUser as User)`; (8) call SDK `upsertUsers` with `[{ id: streamUserId, name: displayName }]` per D-02 (use `client.upsertUsers` if available on `StreamClient`; if TypeScript shows only `video` namespace, use the official Video API upsert for users per current SDK typings); (9) parse validity: `const sec = Number(process.env.STREAM_TOKEN_VALIDITY_SECONDS) || 3600`; (10) `const token = client.generateUserToken({ user_id: streamUserId, validity_in_seconds: sec })`; (11) `const expiresAt = new Date(Date.now() + sec * 1000).toISOString()`; (12) return `NextResponse.json({ token, expiresAt })` with status 200. Do not read `user_id` from request body.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `route.ts` contains `export async function POST`
    - `route.ts` contains `payload.auth`
    - `route.ts` contains `generateUserToken`
    - `route.ts` contains `expiresAt`
    - `route.ts` contains status `401`
  </acceptance_criteria>
  <done>Endpoint matches D-03–D-07; auth enforced</done>
</task>

<task type="auto">
  <name>Task 4: Vitest spec for mapping (STRM-03)</name>
  <files>tests/int/stream-user-id.int.spec.ts</files>
  <read_first>vitest.config.mts, src/lib/stream/user.ts</read_first>
  <action>Add `tests/int/stream-user-id.int.spec.ts` (matches `include` in `vitest.config.mts`). Import `describe/it/expect` from `vitest`. Test that `streamUserIdFromPayloadUser` returns `"42"` for a minimal object `{ id: 42 }` as `User` (cast) and `"abc"` for `{ id: 'abc' }`.</action>
  <verify>pnpm exec vitest run tests/int/stream-user-id.int.spec.ts --config ./vitest.config.mts</verify>
  <acceptance_criteria>
    - File `tests/int/stream-user-id.int.spec.ts` exists
    - Test run exits 0
  </acceptance_criteria>
  <done>STRM-03 has automated verification</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next API | Untrusted; only cookie session proves identity |
| API → Stream | Server uses API secret; never returned to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | S | route.ts | mitigate | Reject unauthenticated with 401; no user_id from client body |
| T-1-04 | I | JSON response | mitigate | Only return JWT to authenticated user; secrets stay in env |
| T-1-05 | E | generateUserToken | mitigate | Short TTL via validity_in_seconds; optional env override |
| T-1-06 | T | upsertUsers | mitigate | Use Stream SDK over custom HTTP |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit`
- `pnpm exec vitest run tests/int/stream-user-id.int.spec.ts --config ./vitest.config.mts`
- Manual: `curl -i -X POST http://localhost:3000/api/stream/token` without Cookie returns 401
</verification>

<source_audit>
| SOURCE | ID | Feature | Plan | Status |
|--------|-----|---------|------|--------|
| GOAL | — | Server auth + token for logged-in user | 01-02 | COVERED |
| REQ | STRM-01 | Server Stream client from env | 01-02 | COVERED |
| REQ | STRM-02 | Authenticated token API | 01-02 | COVERED |
| REQ | STRM-03 | user.id → Stream mapping | 01-02 | COVERED |
| CONTEXT | D-01–D-11 | All decisions | 01-02 | COVERED |
| RESEARCH | — | SDK patterns | 01-02 | COVERED |
</source_audit>

<success_criteria>
- Phase 1 roadmap success criteria satisfied (env server-only, token API, mapping documented in code + test)
</success_criteria>

<output>
After completion, create `.planning/phases/01-stream-foundation/01-02-SUMMARY.md`
</output>
