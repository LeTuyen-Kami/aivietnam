---
phase: 01-stream-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .env.example
  - src/environment.d.ts
autonomous: true
requirements:
  - STRM-01
user_setup:
  - service: getstream
    why: "Stream Video API key and secret are required before the token route can return real JWTs in dev/prod."
    env_vars:
      - name: STREAM_API_KEY
        source: "GetStream dashboard → your app → API keys"
      - name: STREAM_API_SECRET
        source: "GetStream dashboard → same place (server secret)"
must_haves:
  truths:
    - Dependencies for `@stream-io/node-sdk` and `server-only` are declared in package.json.
    - `.env.example` documents STREAM_* and comments for NEXT_PUBLIC_STREAM_API_KEY without real secrets.
  artifacts:
    - package.json lists `@stream-io/node-sdk` and `server-only`.
    - `.env.example` contains lines starting with STREAM_API_KEY and STREAM_API_SECRET (commented placeholders OK).
    - src/environment.d.ts declares optional STREAM_* variables for TypeScript.
  key_links:
    - Lockfile updated with pnpm so CI installs match local.
---

<objective>
Add server-side Stream SDK dependencies, document environment variables (including future public API key for React SDK), and extend TypeScript env typings so Phase 1 implementation has no secret leakage into client bundles.

Purpose: Satisfies preparation for STRM-01 (credentials only on server) and unblocks Plan 01-02.

Output: Updated `package.json` / lockfile, `.env.example`, `src/environment.d.ts`.
</objective>

<execution_context>
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/workflows/execute-plan.md
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-stream-foundation/01-CONTEXT.md
@.planning/phases/01-stream-foundation/01-RESEARCH.md
@.env.example
@src/environment.d.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pnpm dependencies</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>package.json, .planning/phases/01-stream-foundation/01-CONTEXT.md</read_first>
  <action>From repo root run: `pnpm add @stream-io/node-sdk server-only` (exact packages). Do not add `@stream-io/video-react-sdk` in this phase (Phase 4–5). Ensure `engines` and existing scripts unchanged.</action>
  <verify>pnpm install exits 0</verify>
  <acceptance_criteria>
    - `package.json` contains `"@stream-io/node-sdk"` in dependencies
    - `package.json` contains `"server-only"` in dependencies
    - `pnpm-lock.yaml` updated and references both packages
  </acceptance_criteria>
  <done>Lockfile committed-ready; no duplicate or conflicting Stream packages</done>
</task>

<task type="auto">
  <name>Task 2: Document Stream env vars in .env.example</name>
  <files>.env.example</files>
  <read_first>.env.example, .planning/phases/01-stream-foundation/01-CONTEXT.md</read_first>
  <action>Add lines (with comments, no real secrets): `STREAM_API_KEY=`, `STREAM_API_SECRET=`, optional `# STREAM_TOKEN_VALIDITY_SECONDS=3600`. Add comment block that `NEXT_PUBLIC_STREAM_API_KEY` will mirror the public Stream API key in Phase 4–5 for `@stream-io/video-react-sdk` and must not hold secrets. Per D-10, Phase 1 may leave NEXT_PUBLIC line commented only.</action>
  <verify>grep -n STREAM .env.example</verify>
  <acceptance_criteria>
    - `.env.example` contains the substring `STREAM_API_KEY`
    - `.env.example` contains the substring `STREAM_API_SECRET`
    - `.env.example` contains a comment mentioning `NEXT_PUBLIC_STREAM_API_KEY`
  </acceptance_criteria>
  <done>Operators know which vars to copy from GetStream dashboard</done>
</task>

<task type="auto">
  <name>Task 3: Extend environment.d.ts for Stream</name>
  <files>src/environment.d.ts</files>
  <read_first>src/environment.d.ts</read_first>
  <action>In `ProcessEnv`, add optional entries: `STREAM_API_KEY?: string`, `STREAM_API_SECRET?: string`, `STREAM_TOKEN_VALIDITY_SECONDS?: string`, and optional `NEXT_PUBLIC_STREAM_API_KEY?: string`. Do not mark secrets as required (users may not have Stream in minimal dev).</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/environment.d.ts` contains `STREAM_API_KEY`
    - `src/environment.d.ts` contains `STREAM_API_SECRET`
  </acceptance_criteria>
  <done>Typecheck passes with new env keys</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → operators | `.env.example` must never contain real secrets; only names and placeholders |
| Dev machine → npm | Dependencies pulled from registry; lockfile pins versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | I | .env.example | mitigate | Placeholder values only; document dashboard source in user_setup |
| T-1-02 | T | pnpm-lock.yaml | mitigate | Commit lockfile; use official registry |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` passes
- `rg "STREAM_API" .env.example` shows key names
</verification>

<source_audit>
| SOURCE | ID | Feature | Plan | Status |
|--------|-----|---------|------|--------|
| GOAL | — | Env + SDK prep for Stream | 01-01 | COVERED |
| REQ | STRM-01 | Server init from env | 01-01 | COVERED (prep); completion in 01-02 |
| CONTEXT | D-09, D-10 | Env naming + future NEXT_PUBLIC note | 01-01 | COVERED |
</source_audit>

<success_criteria>
- All tasks completed
- STRM-01 documentation path clear; implementation continues in Plan 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-stream-foundation/01-01-SUMMARY.md`
</output>
