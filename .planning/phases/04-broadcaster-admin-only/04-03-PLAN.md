---
phase: 04-broadcaster-admin-only
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/lib/stream/publicClientEnv.ts
  - src/app/(frontend)/broadcaster/[slug]/page.tsx
  - src/app/(frontend)/broadcaster/[slug]/Broadcaster.client.tsx
  - src/environment.d.ts
  - .env.example
  - tests/int/broadcaster-page.int.spec.ts
  - tests/int/broadcaster-client.int.spec.ts
autonomous: true
requirements: [BRD-01, BRD-02]
gap_closure: true
must_haves:
  truths:
    - "Admin can open `/broadcaster/[slug]` and immediately see a deterministic setup error when `NEXT_PUBLIC_STREAM_API_KEY` is missing."
    - "Broadcaster start flow is blocked before publish actions when public Stream API key config is invalid."
    - "Runtime env contract for broadcaster is documented and test-guarded so local/dev setup is not ambiguous."
  artifacts:
    - path: "src/lib/stream/publicClientEnv.ts"
      provides: "Single helper that validates and normalizes `NEXT_PUBLIC_STREAM_API_KEY` usage for frontend broadcaster runtime."
    - path: "src/app/(frontend)/broadcaster/[slug]/page.tsx"
      provides: "Server-side preflight signal for missing broadcaster runtime env before interactive publish flow."
    - path: ".env.example"
      provides: "Explicit uncommented setup contract for `NEXT_PUBLIC_STREAM_API_KEY` aligned with Stream client runtime."
    - path: "tests/int/broadcaster-page.int.spec.ts"
      provides: "Contract test for preflight and setup messaging behavior."
    - path: "tests/int/broadcaster-client.int.spec.ts"
      provides: "Contract test for client runtime guard and publish-action blocking."
  key_links:
    - from: "src/lib/stream/publicClientEnv.ts"
      to: "src/app/(frontend)/broadcaster/[slug]/page.tsx"
      via: "shared env preflight evaluation"
      pattern: "getPublicStreamApiKey|validatePublicStreamEnv"
    - from: "src/lib/stream/publicClientEnv.ts"
      to: "src/app/(frontend)/broadcaster/[slug]/Broadcaster.client.tsx"
      via: "client-side guarded initialization"
      pattern: "NEXT_PUBLIC_STREAM_API_KEY"
    - from: ".env.example"
      to: "broadcaster runtime"
      via: "operator setup guidance"
      pattern: "NEXT_PUBLIC_STREAM_API_KEY="
---

<objective>
Close the Phase 4 runtime gap where broadcaster UI cannot run without `NEXT_PUBLIC_STREAM_API_KEY`, by making the env contract explicit, validated early, and protected by automated tests.

Purpose: remove runtime ambiguity that blocks admin broadcaster operations while preserving admin-only security boundaries from BRD-01/BRD-02.
Output: deterministic env validation path, updated setup contract, and regression tests for broadcaster page/client guards.
</objective>

<execution_context>
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/workflows/execute-plan.md
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-broadcaster-admin-only/04-CONTEXT.md
@.planning/phases/04-broadcaster-admin-only/04-RESEARCH.md
@.planning/phases/04-broadcaster-admin-only/04-VERIFICATION.md
@.planning/phases/04-broadcaster-admin-only/04-HUMAN-UAT.md
@.planning/phases/04-broadcaster-admin-only/04-02-SUMMARY.md
@src/app/(frontend)/broadcaster/[slug]/page.tsx
@src/app/(frontend)/broadcaster/[slug]/Broadcaster.client.tsx
@tests/int/broadcaster-page.int.spec.ts
@tests/int/broadcaster-client.int.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and enforce broadcaster public env contract (per D-01, D-02, D-11)</name>
  <read_first>
    - src/app/(frontend)/broadcaster/[slug]/page.tsx
    - src/app/(frontend)/broadcaster/[slug]/Broadcaster.client.tsx
    - src/environment.d.ts
    - .env.example
  </read_first>
  <files>src/lib/stream/publicClientEnv.ts, src/app/(frontend)/broadcaster/[slug]/page.tsx, src/app/(frontend)/broadcaster/[slug]/Broadcaster.client.tsx, src/environment.d.ts, .env.example</files>
  <behavior>
    - Test 1: Missing `NEXT_PUBLIC_STREAM_API_KEY` produces deterministic preflight/setup error state for broadcaster route.
    - Test 2: Publish actions remain disabled when public Stream API key is absent/blank.
    - Test 3: Valid non-empty key allows normal broadcaster initialization path.
  </behavior>
  <action>Create `src/lib/stream/publicClientEnv.ts` as the single normalization/validation helper for `NEXT_PUBLIC_STREAM_API_KEY` (trim + non-empty check + user-facing setup message). Refactor broadcaster page/client to consume this helper so env failure is surfaced early and consistently (not ad-hoc string checks). Keep admin role enforcement unchanged (D-02, D-11) and do not weaken existing server authorization flow from 04-01/04-02. Update `.env.example` from commented guidance to explicit setup contract line for `NEXT_PUBLIC_STREAM_API_KEY=` with clear note that value mirrors `STREAM_API_KEY` public key.</action>
  <verify>
    <automated>bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>Broadcaster route/client no longer rely on scattered env checks; missing key produces one consistent setup path and no publish action can proceed without a valid public key.</acceptance_criteria>
  <done>Runtime env requirement is enforced early, typed, and documented with zero ambiguity for operators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for broadcaster env guard and setup messaging</name>
  <read_first>
    - tests/int/broadcaster-page.int.spec.ts
    - tests/int/broadcaster-client.int.spec.ts
    - src/lib/stream/publicClientEnv.ts
  </read_first>
  <files>tests/int/broadcaster-page.int.spec.ts, tests/int/broadcaster-client.int.spec.ts</files>
  <behavior>
    - Test 1: page contract asserts preflight/setup handling for missing public Stream key.
    - Test 2: client contract asserts start control is blocked and setup messaging appears when key is missing.
    - Test 3: client contract still includes token/start/end orchestration links for valid runtime path.
  </behavior>
  <action>Extend existing broadcaster integration contract tests to assert the new env-validation path and messaging semantics from Task 1. Keep tests focused on deterministic source contracts (matching current test style) and include explicit assertions that start/publish control cannot proceed under missing-key conditions. Preserve existing BRD-01/BRD-02 route and endpoint wiring assertions so this gap fix does not regress admin-only broadcaster behavior.</action>
  <verify>
    <automated>bun run test:int tests/int/broadcaster-page.int.spec.ts tests/int/broadcaster-client.int.spec.ts</automated>
  </verify>
  <acceptance_criteria>Automated tests fail if env guard/messaging is removed or if broadcaster controls become startable without required public key.</acceptance_criteria>
  <done>Gap has automated regression coverage tied to the exact runtime failure mode reported in HUMAN-UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/broadcaster/[slug]` SSR | Untrusted client request enters admin-gated broadcaster route |
| browser → `/api/stream/broadcaster-token` | Privileged token mint path must remain admin-only |
| runtime env → client initialization | Missing/malformed env can cause undefined behavior and operational denial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | E | `src/app/(frontend)/broadcaster/[slug]/page.tsx` | mitigate | Preserve existing server admin checks and ensure env handling does not bypass role gates (per D-11). |
| T-04-03-02 | D | `src/app/(frontend)/broadcaster/[slug]/Broadcaster.client.tsx` | mitigate | Add deterministic missing-env preflight and block start flow when key invalid to prevent repeated runtime failures. |
| T-04-03-03 | T | `.env.example` / setup contract | mitigate | Document exact env variable and expected value mapping to avoid misconfiguration drift across environments. |
</threat_model>

<verification>
- `bun run tsc --noEmit`
- `bun run test:int tests/int/broadcaster-page.int.spec.ts tests/int/broadcaster-client.int.spec.ts`
</verification>

<success_criteria>
- HUMAN-UAT gap “Missing Stream API key blocks broadcaster UI” is closed with explicit env contract and early enforcement.
- Broadcaster runtime behavior is deterministic for both missing-key and valid-key states.
- BRD-01/BRD-02 protections remain intact while closing operational setup failure.
</success_criteria>

<output>
After completion, create `.planning/phases/04-broadcaster-admin-only/04-03-SUMMARY.md`
</output>
