---
phase: 03-admin-session-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Livestreams/index.ts
  - src/utilities/livestreamViewerUrl.ts
  - src/components/Livestreams/LivestreamStatusCell.tsx
  - src/components/Livestreams/LivestreamSlugActionsCell.tsx
  - src/components/Livestreams/LivestreamViewerLinksField.tsx
autonomous: true
requirements:
  - ADM-01
  - ADM-02
user_setup: []
must_haves:
  truths:
    - List view shows columns per CONTEXT D-01 (`title`, `slug`, `status`, `scheduledAt`, `updatedAt`) and default sort is most recently updated first (`-updatedAt`) per D-02.
    - `status` list cells use semantic styling per D-03 / UI-SPEC.
    - Operators can open public viewer URL in a new tab and copy full absolute URL per D-05 from both list (slug column) and document view (preview + optional ui field) per D-07–D-08.
    - Preview/livePreview URLs target `/live/[slug]` public path, not `generatePreviewPath` / draft preview per D-07.
  artifacts:
    - New utility module under `src/utilities/` for viewer path + absolute URL.
    - New admin components under `src/components/Livestreams/` referenced from collection config.
    - Updated `src/collections/Livestreams/index.ts` with `defaultSort`, `defaultColumns`, field `admin.components.Cell`, `admin.preview`, `admin.livePreview`, and at least one `ui` field for document-level viewer actions.
  key_links:
    - Viewer path pattern `/live/[slug]` matches Phase 2 CONTEXT; 404 until Phase 5 is acceptable.
---

<objective>
Deliver Payload Admin UX for **`livestreams`**: scannable list (columns, sort, status styling), operational **viewer URLs** (open + copy, preview buttons) per **03-CONTEXT.md** D-01–D-08 and **03-UI-SPEC.md**. Does not implement the public `/live/[slug]` page (Phase 5).

Output: utility module, React admin components, updated `Livestreams` collection config, regenerated import map and types.
</objective>

<execution_context>
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/workflows/execute-plan.md
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-admin-session-management/03-CONTEXT.md
@.planning/phases/03-admin-session-management/03-RESEARCH.md
@.planning/phases/03-admin-session-management/03-PATTERNS.md
@.planning/phases/03-admin-session-management/03-UI-SPEC.md
@src/collections/Posts/index.ts
@src/collections/Livestreams/index.ts
@src/utilities/getURL.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viewer URL helpers</name>
  <files>src/utilities/livestreamViewerUrl.ts</files>
  <read_first>src/utilities/getURL.ts, .planning/phases/03-admin-session-management/03-CONTEXT.md (D-04, D-05)</read_first>
  <action>Create `src/utilities/livestreamViewerUrl.ts` exporting: (1) `getLivestreamViewerPath(slug: string | null | undefined)` returning `null` if falsy, otherwise string `/live/${encodeURIComponent(String(slug).trim())}`; (2) `getLivestreamViewerAbsoluteUrl(slug: string | null | undefined)` returning `null` if path is null, else concatenate `getServerSideURL()` with `path` after removing one trailing slash from the base URL if present (import `getServerSideURL` from `@/utilities/getURL` or equivalent path alias used in repo). Do not import `generatePreviewPath`.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - File `src/utilities/livestreamViewerUrl.ts` exists and exports `getLivestreamViewerPath` and `getLivestreamViewerAbsoluteUrl`
    - Same file contains substring `/live/` and `encodeURIComponent`
    - Same file imports from path containing `getURL` or `getServerSideURL`
  </acceptance_criteria>
  <done>Helpers ready for preview and cells</done>
</task>

<task type="auto">
  <name>Task 2: Status and slug admin cells</name>
  <files>src/components/Livestreams/LivestreamStatusCell.tsx, src/components/Livestreams/LivestreamSlugActionsCell.tsx</files>
  <read_first>src/collections/Livestreams/index.ts, .planning/phases/03-admin-session-management/03-UI-SPEC.md (status table)</read_first>
  <action>Create Payload list **Cell** components: (1) **Status** — render raw status label with CSS classes or inline styles using Payload theme variables (`var(--theme-*)`) so `draft` / `scheduled` / `live` / `ended` are visually distinct per UI-SPEC. (2) **Slug** — show slug text; buttons/links for **Open viewer** (href = `getLivestreamViewerAbsoluteUrl(cellValue)` in new tab with `rel="noopener noreferrer"`) and **Copy URL** calling `navigator.clipboard.writeText` with the absolute URL; use `'use client'` on the slug component if using clipboard. Use Payload `DefaultCell` or `useField` patterns from Payload 3 docs if required for cell props typing.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/Livestreams/LivestreamStatusCell.tsx` exports a component used as string path from collection config
    - `src/components/Livestreams/LivestreamSlugActionsCell.tsx` contains `use client` OR documents server-only pattern if clipboard is split to a child client chunk
    - At least one file imports from `@/utilities/livestreamViewerUrl` or relative path to `livestreamViewerUrl.ts`
  </acceptance_criteria>
  <done>List row shows status + slug actions</done>
</task>

<task type="auto">
  <name>Task 3: Document viewer UI field + collection admin config</name>
  <files>src/components/Livestreams/LivestreamViewerLinksField.tsx, src/collections/Livestreams/index.ts</files>
  <read_first>src/collections/Posts/index.ts (preview/livePreview), src/collections/Livestreams/index.ts, src/utilities/livestreamViewerUrl.ts</read_first>
  <action>3a) Add optional **`ui`** field (e.g. name `viewerOps`) on `livestreams` with `admin.components.Field` pointing to `LivestreamViewerLinksField` — read-only panel showing absolute viewer URL, **Open** + **Copy** (same behavior as list). 3b) On **`status`** and **`slug`** fields, set `admin.components.Cell` to the new Cell paths (paths relative to project root / import map base per AGENTS.md). 3c) Set `admin.defaultColumns` to `['title', 'slug', 'status', 'scheduledAt', 'updatedAt']` per D-01. 3d) Set collection-level **`defaultSort: '-updatedAt'`** per D-02. 3e) Set **`admin.preview`** and **`admin.livePreview`** using `getLivestreamViewerAbsoluteUrl(data?.slug)` or path variant consistent with Payload API — return string URL for preview buttons; must **not** call `generatePreviewPath`. Match `Posts` callback shapes (`livePreview.url`, `preview` function signature).</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/collections/Livestreams/index.ts` contains `defaultSort: '-updatedAt'`
    - Same file contains `defaultColumns` array including `scheduledAt` in order consistent with D-01
    - Same file contains `livePreview` and `preview` and neither imports `generatePreviewPath`
    - Same file references `LivestreamStatusCell` and `LivestreamSlugActionsCell` via string paths under `src/components/Livestreams/`
    - A `type: 'ui'` field exists with `name` matching Field component wiring
  </acceptance_criteria>
  <done>Admin list + document match CONTEXT decisions</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Import map, types, typecheck</name>
  <files>src/app/(payload)/admin/importMap.js (generated), src/payload-types.ts (generated)</files>
  <read_first>package.json scripts, AGENTS.md (import map)</read_first>
  <action>From repo root run: `pnpm run generate:importmap` then `pnpm run generate:types` then `pnpm exec tsc --noEmit`. Fix any path or type errors from new components. If `DATABASE_URL` is available and Payload reports pending schema changes from this work, run `CI=true PAYLOAD_MIGRATING=true pnpm payload migrate`; if no schema delta, migration may no-op — capture output in SUMMARY if skipped.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - Last command `pnpm exec tsc --noEmit` exits 0
    - `pnpm run generate:importmap` completes with exit 0
    - `pnpm run generate:types` completes with exit 0
  </acceptance_criteria>
  <done>Repo builds; admin can load new components</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Admin → operator | Only admins manage `livestreams` (existing access unchanged) |
| URL → browser | Viewer URLs are **public by design** (Phase 5); must not embed secrets |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | I | Slug in href | mitigate | `encodeURIComponent` in `getLivestreamViewerPath`; strip/null unsafe slug |
| T-3-02 | E | Clipboard | mitigate | Copy only **public** absolute URL; no tokens in viewer link |

</threat_model>

<verification>
## Goal-backward checks

- [ ] ADM-01: List shows sessions with scannable status + columns per roadmap
- [ ] ADM-02: Open + copy viewer URL available from admin
- [ ] `pnpm exec tsc --noEmit` passes after import map + types
- [ ] Manual: Payload Admin → Livestreams — visual check status + buttons (document in UAT)

## Multi-source coverage audit

| Source | Item | Plan coverage |
|--------|------|----------------|
| GOAL | Operator manages sessions in Admin | Tasks 2–3 |
| REQ | ADM-01 | Tasks 2–3 |
| REQ | ADM-02 | Tasks 1–3 |
| CONTEXT | D-01–D-08 | Tasks 1–3 |
| RESEARCH | Cells, preview, import map | Tasks 2–4 |

</verification>

---

## PLANNING COMPLETE
