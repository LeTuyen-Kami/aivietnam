---
phase: 02-payload-session-model
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/collections/Livestreams/index.ts
  - src/payload-types.ts
autonomous: true
requirements:
  - CMS-01
  - CMS-02
user_setup: []
must_haves:
  truths:
    - `access.read` returns `false` when no user; admin reads all; authenticated non-admin gets Where `{ status: { not_equals: 'draft' } }`.
    - `create`/`update`/`delete` remain admin-only via `isUsersCollectionAdmin`.
    - `pnpm generate:types` run after code; `pnpm exec tsc --noEmit` passes.
    - Database schema applied via Payload migrate before treating phase as verified.
  artifacts:
    - `src/collections/Livestreams/index.ts` encodes read rules above (grep-verifiable strings).
    - Regenerated `payload-types.ts` includes `Livestreams` types.
  key_links:
    - Hooks that call Local API pass `req` and `overrideAccess: false` when user-scoped.
---

<objective>
Replace interim access with final CMS-02 rules (D-06–D-10): anonymous reads denied; admins full access; authenticated members read non-draft sessions only. Add `beforeChange` or `beforeValidate` hook only if needed for slug collision or empty slug fill — use `slugifyTitle` per D-12; pass `req` to nested calls. Run **[BLOCKING]** DB migration, then `pnpm generate:types`.

Purpose: Completes CMS-01 + CMS-02 and prepares types for Phases 3–5.

Output: Updated collection module, migrated DB, regenerated `payload-types.ts`.
</objective>

<execution_context>
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/workflows/execute-plan.md
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-payload-session-model/02-CONTEXT.md
@.planning/phases/02-payload-session-model/02-RESEARCH.md
@.planning/phases/02-payload-session-model/02-PATTERNS.md
@AGENTS.md
@src/collections/Livestreams/index.ts
</context>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files (`src/collections/**/*.ts`). The executor MUST complete a **[BLOCKING]** task that runs the database schema push **after** all collection access/hook code is merged and **before** final verification.

- ORM detected: Payload CMS (Postgres)
- Push command: `CI=true PAYLOAD_MIGRATING=true pnpm payload migrate`
- Non-TTY workaround: prefix with `CI=true PAYLOAD_MIGRATING=true` per README / GSD references
- If migrate fails (e.g. no `DATABASE_URL`), mark task `autonomous: false` and document operator action

This task is mandatory — build/type checks alone are insufficient for DB truth.
</schema_push_requirement>

<tasks>

<task type="auto">
  <name>Task 1: Implement final access control and optional slug hook</name>
  <files>src/collections/Livestreams/index.ts</files>
  <read_first>src/collections/Livestreams/index.ts, .planning/phases/02-payload-session-model/02-CONTEXT.md, AGENTS.md</read_first>
  <action>Update `access`: `create`, `update`, `delete` → `({ req: { user } }) => isUsersCollectionAdmin(user)`. `read` → if `!user` return `false`; if `isUsersCollectionAdmin(user)` return `true`; else return `{ status: { not_equals: 'draft' } }` (exact Payload Where shape). If adding hooks that call `req.payload.*`, pass `req` and use `overrideAccess: false` when passing `user`. Optional: `beforeValidate` to append suffix on slug collision — only if implementer chooses D-13 path; otherwise rely on unique index error.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/collections/Livestreams/index.ts` contains `not_equals: 'draft'`
    - Same file contains `if (!user)` or equivalent guard returning false for anonymous
    - Same file shows `isUsersCollectionAdmin` for create/update/delete
  </acceptance_criteria>
  <done>Access matches CONTEXT D-06–D-09</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run Payload migrate</name>
  <files>(database)</files>
  <read_first>README.md (Payload migrate section)</read_first>
  <action>From repo root run: `CI=true PAYLOAD_MIGRATING=true pnpm payload migrate`. Requires valid `DATABASE_URL` in environment. If non-interactive env needs only migration push, use env vars as per project README.</action>
  <verify>Command exits 0</verify>
  <acceptance_criteria>
    - Terminal shows migrate completed without error OR documented blocker for missing DB
  </acceptance_criteria>
  <done>Postgres schema includes `livestreams` storage</done>
</task>

<task type="auto">
  <name>Task 3: Generate Payload types</name>
  <files>src/payload-types.ts</files>
  <read_first>package.json scripts</read_first>
  <action>Run `pnpm generate:types` (same as `payload generate:types` per package.json). Commit regenerated `src/payload-types.ts` with `Livestreams` interface/collection types.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/payload-types.ts` contains `livestreams` (collection slug in generated types — search lowercase table/collection name as emitted)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Types align with collection schema</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous → API | No read without `req.user` |
| Member → CMS | Members see only non-draft rows |
| Admin → CMS | Full CRUD + draft visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03 | I | access.read | mitigate | Explicit `false` for unauthenticated; Where for draft exclusion |
| T-2-04 | E | Local API | mitigate | Document `overrideAccess: false` in hooks when user passed |
| T-2-05 | T | DB drift | mitigate | [BLOCKING] `pnpm payload migrate` after schema change |
</threat_model>

<verification>
## Goal-backward checks

- [ ] CMS-02 read rules implemented and grep-verifiable
- [ ] Migrate run successfully (or explicit env blocker recorded)
- [ ] `pnpm generate:types` + `pnpm exec tsc --noEmit` pass
</verification>
