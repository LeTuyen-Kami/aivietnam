---
phase: 02-payload-session-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Livestreams/index.ts
  - src/payload.config.ts
autonomous: true
requirements:
  - CMS-01
user_setup: []
must_haves:
  truths:
    - Collection slug is `livestreams` and includes `title`, unique `slug`, `status`, `callId`, `callType` with default `livestream`, optional `description` and `scheduledAt`, `timestamps: true`.
    - `slug` uses `slugField` with `slugifyTitle` from `@/utilities/slugify` (per D-11, D-12).
    - Collection is imported and listed in `src/payload.config.ts` `collections` array.
  artifacts:
    - `src/collections/Livestreams/index.ts` exports `Livestreams` (or equivalent) `CollectionConfig<'livestreams'>`.
    - `src/payload.config.ts` imports and includes `Livestreams`.
  key_links:
    - Field set matches CONTEXT D-02–D-05; status options `draft` | `scheduled` | `live` | `ended`.
---

<objective>
Define the `livestreams` Payload collection with fields and indexes required for CMS-01 (call metadata, title, status, public slug). Register it in `payload.config.ts`. Use **interim** access: only admins may read/create/update/delete (`isUsersCollectionAdmin`) so the schema is safe until Plan 02-02 implements member read rules (D-06–D-08).

Purpose: Delivers CMS-01 structure and enables DB migration in 02-02.

Output: New `src/collections/Livestreams/index.ts`, updated `src/payload.config.ts`.
</objective>

<execution_context>
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/workflows/execute-plan.md
@/Volumes/ExternalSSD/dev/aivietnam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-payload-session-model/02-CONTEXT.md
@.planning/phases/02-payload-session-model/02-RESEARCH.md
@.planning/phases/02-payload-session-model/02-PATTERNS.md
@src/collections/Posts/index.ts
@src/utilities/slugify.ts
@src/payload.config.ts
@src/access/isAdminUser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Livestreams collection module</name>
  <files>src/collections/Livestreams/index.ts</files>
  <read_first>src/collections/Posts/index.ts, src/utilities/slugify.ts, .planning/phases/02-payload-session-model/02-CONTEXT.md</read_first>
  <action>Create `src/collections/Livestreams/index.ts` exporting `CollectionConfig<'livestreams'>` with slug `'livestreams'`, `admin.useAsTitle: 'title'`, `timestamps: true`. Fields: `title` (text, required); `slugField({ slugify: ({ valueToSlugify }) => slugifyTitle(valueToSlugify) })` imported from `payload` and `@/utilities/slugify`; `status` select required with `draft`, `scheduled`, `live`, `ended` (labels optional); `callId` text required; `callType` text required defaultValue `'livestream'`; `description` textarea optional; `scheduledAt` date optional. Interim `access`: `create`, `update`, `delete`, `read` all `({ req: { user } }) => isUsersCollectionAdmin(user)` using `isUsersCollectionAdmin` from `@/access/isAdminUser`. Do not add versions/drafts plugin unless CONTEXT changes.</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - File `src/collections/Livestreams/index.ts` contains the substring `slug: 'livestreams'`
    - Same file contains `callType` and `defaultValue: 'livestream'` (or equivalent Payload default for callType field)
    - Same file contains `isUsersCollectionAdmin`
    - Same file contains `slugField`
  </acceptance_criteria>
  <done>Collection compiles with interim admin-only access</done>
</task>

<task type="auto">
  <name>Task 2: Register Livestreams in payload config</name>
  <files>src/payload.config.ts</files>
  <read_first>src/payload.config.ts, src/collections/Livestreams/index.ts</read_first>
  <action>Import `Livestreams` from `./collections/Livestreams` (path style must match other collections). Append `Livestreams` to the `collections` array in the same file as other collection imports — preserve alphabetical or existing ordering used by the repo (match neighboring imports).</action>
  <verify>pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/payload.config.ts` contains `Livestreams` in the import section from `./collections/Livestreams` or `@/collections/Livestreams` consistent with project alias usage
    - `src/payload.config.ts` array `collections` includes `Livestreams`
  </acceptance_criteria>
  <done>Config references new collection for migration step in 02-02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS → DB | New table/columns; only admins can write in interim model |
| Repo → operator | No secrets in collection file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | E | access (interim) | mitigate | All operations require admin until 02-02 implements least-privilege read |
| T-2-02 | I | slug | mitigate | Unique slug via slugField; collision handling in 02-02 hooks if needed |
</threat_model>

<verification>
## Goal-backward checks

- [ ] CMS-01 field minimum (`callId`, `callType`, title, status, slug) present in collection file
- [ ] `livestreams` registered in `payload.config.ts`
- [ ] `pnpm exec tsc --noEmit` passes
</verification>
